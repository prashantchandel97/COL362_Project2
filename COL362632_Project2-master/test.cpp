#include<bits/stdc++.h>
using namespace std;
int main()
{
  vector<vector<int>> vect(2,vector<int>(0,0));
  vect[0].push_back(01);
  vect[1].push_back(11);
  for (auto iterator = vect[0].begin(); iterator != vect[0].end(); ++iterator)
	 {		 cout << *iterator << "\n";
 }
}
//huehue
